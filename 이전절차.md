# 제로트래픽 — 버셀 이전 절차

작성일: 2026-07-30

---

## 0. 무엇이 바뀌었나

| 항목 | Netlify | Vercel |
|---|---|---|
| 함수 폴더 | `netlify/functions/` | `api/` |
| API 경로 | `/.netlify/functions/store` | `/api/store` |
| 데이터 저장소 | Netlify Blobs | Upstash Redis |
| 스케줄 | 함수 안의 `export const config` | `vercel.json`의 `crons` |
| 방문자 IP 헤더 | `x-nf-client-connection-ip` | `x-real-ip` |

**로직은 하나도 바꾸지 않았습니다.** 혼잡도 계산, 영업시간 차단, 10분 메모리 캐시,
IP 레이트리밋, 데모 폴백, 제로트래픽 시각 계산 모두 그대로입니다.

> Vercel KV는 2024년 12월에 폐지되어 Upstash Redis로 통합되었습니다.
> 버셀 마켓플레이스에서 클릭으로 설치되고 환경변수도 자동 주입되므로,
> 실제 사용 경험은 KV와 거의 같습니다.

---

## 1. GitHub에 올리기

기존 `media-jungle/zerotraffic` 저장소에 그대로 올리면 됩니다.
단 **기존 파일 중 아래는 삭제**해야 합니다 (버셀에서는 쓰이지 않음).

- `netlify.toml`
- `netlify/functions/` 폴더 전체

새로 올릴 것: `api/`, `public/`, `package.json`, `vercel.json`, `.gitignore`

> 저장소를 새로 만들어도 됩니다. 그 경우 Netlify 쪽은 그대로 두고
> 새 저장소를 버셀에 연결하면, 전환 실패 시 되돌리기가 쉽습니다. (권장)

---

## 2. 버셀 프로젝트 만들기

1. vercel.com → Add New → Project
2. GitHub 저장소 선택
3. Framework Preset: **Other** (프레임워크 없는 정적 + 함수 구성)
4. Deploy

---

## 3. Upstash Redis 붙이기

1. 버셀 프로젝트 → **Storage** 탭 → Marketplace에서 **Upstash for Redis** 설치
2. 데이터베이스 생성 후 이 프로젝트에 연결
3. 연결하면 `KV_REST_API_URL`, `KV_REST_API_TOKEN` 환경변수가 자동으로 주입됩니다

---

## 4. 환경변수 넣기 (버셀 → Settings → Environment Variables)

| 이름 | 용도 |
|---|---|
| `NCP_CLIENT_ID` | 네이버 클라우드 Maps 키 ID |
| `NCP_CLIENT_SECRET` | 네이버 클라우드 Maps 시크릿 |
| `DATA_GO_KR_KEY` | 공공데이터포털 인증키 (공항 주차장 / 출국 인원) |
| `TOUR_API_KEY` | 한국관광공사 TourAPI 키 (전시장 행사) |
| `CRON_SECRET` | 아무 긴 문자열 (자동수집 함수 무단 호출 차단용) |

> 네이버 클라우드 콘솔 → Application → **Web 서비스 URL 허용목록**에
> 새 버셀 도메인을 추가해야 합니다. 이걸 빠뜨리면 혼잡도가 데모값으로 표시됩니다.

---

## 5. 백업 데이터 복원 (한 번만)

```bash
# 프로젝트 폴더에서
npm install
vercel link          # 방금 만든 버셀 프로젝트에 연결
vercel env pull .env.local

# 백업 폴더를 이 폴더 안에 복사한 뒤
node --env-file=.env.local restore-to-redis.mjs
```

`blobs-backup` 폴더(125개 파일)가 이 프로젝트 폴더 안에 있어야 합니다.

복원 키 형식이 앱이 읽는 키와 동일하게 맞춰져 있으므로, 복원 후 바로 인식됩니다.

---

## 6. 확인할 것

- [ ] 웹사이트가 열리고 카테고리 카드가 보이는지
- [ ] 코스트코 지점을 탭했을 때 **실제 혼잡도**가 뜨는지
      (데모값이면 → `NCP_CLIENT_ID` 또는 네이버 URL 허용목록 문제)
- [ ] 공항 주차장 잔여 대수가 뜨는지 (→ `DATA_GO_KR_KEY`)
- [ ] 복원한 지점에서 '제로트래픽 시각'이 표시되는지
- [ ] `https://` 자물쇠가 정상인지 (버셀은 인증서 자동 발급)

---

## 7. 도메인 옮기기 (웹사이트가 확인된 뒤에)

1. 버셀 프로젝트 → Settings → Domains → `zerotraffic.kr` 추가
2. 버셀이 알려주는 값으로 **가비아 DNS 레코드를 교체**
   (현재 A 레코드 `75.2.60.5`는 Netlify 주소이므로 버셀 값으로 변경)
3. 버셀이 인증서를 자동 발급 — Netlify에서 겪은 문제가 여기서 해소될 것으로 기대

> 도메인을 옮기기 전까지는 버셀 임시 주소(`...vercel.app`)로 테스트하면 됩니다.
> 그동안 기존 Netlify 사이트는 계속 살아 있습니다.

---

## 8. 미니앱(.ait) 마지막 처리

현재 토스 미니앱은 API를 `https://zerotraffic.netlify.app`으로 호출합니다.
버셀 이전이 안정화되면 그 주소를 새 도메인으로 바꿔 `.ait`를 다시 빌드해
콘솔에 새 버전으로 올려야 합니다.

**순서 주의**: Netlify 사이트를 내리기 전에 미니앱 새 버전이 먼저 출시되어야
미니앱 사용자가 끊기지 않습니다.

---

## 9. 남겨둔 것 / 주의사항

- `collect-evening`은 **2026년 8월 20일 자정 이후 자동 중단**됩니다
  (`api/collect-evening.mjs`의 `COLLECT_END`). 연장하려면 이 날짜만 수정.
- 버셀 크론은 무료 플랜에서 **하루 실행 횟수 제한**이 있습니다.
  현재 설정은 하루 10회(KST 17:00~21:30, 30분 간격)입니다.
  제한에 걸리면 `vercel.json`의 `schedule`을 줄이세요 (예: 1시간 간격).
- 분석 코드(GA4 `G-H1NTXG1M87`, Clarity `xph5ujivry`)는 `index.html`에 그대로 있습니다.
- Netlify 계정은 8월 15일까지 배포가 정지된 상태이므로, 그동안 Netlify 쪽
  코드 수정·재배포는 불가능합니다. 기존 배포본은 계속 서비스됩니다.
